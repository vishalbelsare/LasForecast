## Submission

**New submission.** This is the first submission of LasForecast to CRAN. 

LasForecast (version 1.0.0) implements Lasso-type estimators for predictive regressions in data-rich environments, together with parameter tuning, rolling-window forecasting, backtesting, and visualization.

## Test environments

* Local: macOS 15 (aarch64-apple-darwin20), R 4.5.3
* GitHub Actions: ubuntu-latest, R release

## R CMD check results

`R CMD check` produces:

0 errors | 0 warnings | 0 notes

## Downstream dependencies

There are no downstream dependencies, as this is a new package.
