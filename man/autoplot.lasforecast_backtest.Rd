% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_functions.R
\name{autoplot.lasforecast_backtest}
\alias{autoplot.lasforecast_backtest}
\title{Autoplot method for lasforecast_backtest objects}
\usage{
\method{autoplot}{lasforecast_backtest}(
  object,
  type = c("bar", "heatmap", "forecasts", "frequency", "coef_heatmap", "coef_path"),
  methods = NULL,
  loss = NULL,
  ratio = FALSE,
  top_n = 20,
  standardize = NULL,
  ...
)
}
\arguments{
\item{object}{An object of class \code{"lasforecast_backtest"}.}

\item{type}{Type of plot. Loss-metric plots: \code{"bar"}, \code{"heatmap"}.
Forecast plot: \code{"forecasts"}. Variable selection plots:
\code{"frequency"} (selection frequency bars), \code{"coef_heatmap"}
(time x variable coefficient heatmap), \code{"coef_path"} (coefficient
evolution over time).}

\item{methods}{Character vector of methods to include. Default uses all.}

\item{loss}{Character vector of loss metrics to include (e.g., \code{"RMSE"},
\code{"MAE"}). Used by \code{"bar"} and \code{"heatmap"} types.}

\item{ratio}{Logical. If \code{TRUE}, plot ratios relative to the benchmark
instead of raw metric values. Default \code{FALSE}.}

\item{top_n}{Number of top variables to show for variable selection plots
(default 20).}

\item{standardize}{Logical, forwarded to \code{\link{plot_variable_importance}}
for the variable-selection plot types (\code{"frequency"},
\code{"coef_heatmap"}, \code{"coef_path"}): if \code{TRUE}, each coefficient
is scaled by its predictor's standard deviation. Default \code{NULL} uses
the per-type default.}

\item{...}{Additional arguments passed through (e.g., \code{methods_to_plot}
and \code{date_range} for \code{type = "forecasts"}).}
}
\description{
Autoplot method for lasforecast_backtest objects
}
