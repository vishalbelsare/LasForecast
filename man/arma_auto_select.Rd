% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate.R
\name{arma_auto_select}
\alias{arma_auto_select}
\title{Automatic ARMA order selection by information criterion}
\usage{
arma_auto_select(y, fit_arima, d = 0, max_p = 5, max_q = 5, ic = "aic")
}
\arguments{
\item{y}{Numeric response vector.}

\item{fit_arima}{A fitting closure returning an \code{arima} object or
\code{NULL} on failure.}

\item{d}{Differencing order (held fixed).}

\item{max_p, max_q}{Maximum AR and MA orders.}

\item{ic}{\code{"aic"} or \code{"bic"}.}
}
\value{
A list with \code{fit} (best \code{arima} object, or \code{NULL} if
  every candidate failed) and \code{order} (the selected \code{c(p, d, q)}).
}
\description{
Internal grid search over AR and MA orders (with \code{d} held fixed) used by
\code{\link{arma}} when \code{order} is not supplied. The criterion is built
from the maximized log-likelihood for comparability across orders:
\eqn{-2\ell + 2k} (AIC) or \eqn{-2\ell + \log(n)\,k} (BIC), where \eqn{k} is
the number of estimated parameters including the innovation variance.
}
\keyword{internal}
